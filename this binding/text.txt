when we use a function declaration instead of arrow function inside class component then we got error that can not read property of undefined as we know that we are using this keyword here and an arrow function do not have there own this so it will use the parent this keyword but a normal function has its own this and it will use his own this so to use parents this we need to use bind method 